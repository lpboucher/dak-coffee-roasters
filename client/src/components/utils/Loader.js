import React, { Fragment } from 'react';

const Loader = () => {
    return (
        <Fragment>
            <div className="loader-container">
                <h3>Loading...</h3>
            </div>
        </Fragment>
    );
};

export default Loader;