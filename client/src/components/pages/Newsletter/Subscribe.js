import React from 'react';

import NewsletterPageContainer from '../../container/Newsletter/NewsletterPageContainer';

import { Box } from 'grommet';

const Subscribe = () => {
    return (
        <Box pad={{"top": '160px'}} >
            <NewsletterPageContainer/>
        </Box>
    );
};

export default Subscribe;